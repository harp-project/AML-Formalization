name: Auto update stdpp

on:
  schedule:
    # * is a special character in YAML so you have to quote this string
    # Every Tuesday at 4:15 am
    - cron:  '15 4 * * 2'

jobs:
  stdpp-auto-update:
    name: Stdpp auto update
    runs-on: ubuntu-latest
    steps:
      - run: ./scripts/update-deps
